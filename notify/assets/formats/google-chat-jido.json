{
    "cards": [
        {
            "header": {
                "title": "${PARAMS_TITLE}",
                "subtitle": "${PARAMS_SUBTITLE}",
                "imageUrl": "${PARAMS_ICON}",
                "imageStyle": "IMAGE"
            },
            "sections": [
                {
                    "widgets": [
                        {
                            "keyValue": {
                                "topLabel": "Pipeline",
                                "content": "${BUILD_TEAM_NAME}",
                                "contentMultiline": "false",
                                "bottomLabel": "${BUILD_PIPELINE_NAME}",
                                "onClick": {
                                    "openLink": {
                                        "url": "${JIDO_HOST}/teams/${BUILD_TEAM_NAME}/pipelines/${BUILD_PIPELINE_NAME}"
                                    }
                                }
                            }
                        },
                        {
                            "textParagraph": {
                                "text": "${PARAMS_MESSAGE}"
                            }
                        }
                    ]
                },
                {
                    "widgets": [
                        {
                            "buttons": [
                                {
                                    "textButton": {
                                        "text": "OPEN BUILD ${BUILD_NAME}",
                                        "onClick": {
                                            "openLink": {
                                                "url": "${JIDO_HOST}/teams/${BUILD_TEAM_NAME}/pipelines/${BUILD_PIPELINE_NAME}/jobs/${BUILD_JOB_NAME}/builds/${BUILD_NAME}"
                                            }
                                        }
                                    }
                                }
                            ]
                        }
                    ]
                }
            ]
        }
    ]
}